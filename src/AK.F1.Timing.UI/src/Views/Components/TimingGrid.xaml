<!-- DataContext: AK.F1.Timing.Model.Session -->
<UserControl x:Class="AK.F1.Timing.UI.Views.Components.TimingGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <GroupBox>
        <GroupBox.Header>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding SessionType}"/>
                <TextBlock Text=" Timing"/>
            </StackPanel>
        </GroupBox.Header>
        <DockPanel VerticalAlignment="Stretch">
            <ContentControl Content="{Binding Grid}" DockPanel.Dock="Top"/>
            <Grid Margin="4,4,4,0" DockPanel.Dock="Bottom" VerticalAlignment="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Column="0" Grid.Row="0" HorizontalAlignment="Left" Style="{StaticResource GridSessionText}" Text="{Binding ElapsedSessionTime}"/>
                <TextBlock Grid.Column="1" Grid.Row="0" Foreground="{Binding Path=SessionStatus, Converter={StaticResource SessionStatusToColourConverter}}" HorizontalAlignment="Center" Style="{StaticResource GridSessionText}" Text="{Binding Path=SessionStatus, Converter={StaticResource SessionStatusConverter}}"/>
                <TextBlock Grid.Column="2" Grid.Row="0" HorizontalAlignment="Right" Style="{StaticResource GridSessionText}" Text="{Binding RemainingSessionTime}"/>
            </Grid>
        </DockPanel>
    </GroupBox>
</UserControl>
