<!-- DataContext: AK.F1.Timing.Model.Session.WeatherModel -->
<UserControl x:Class="AK.F1.Timing.UI.Views.Components.MiniWeather"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:AK.F1.Timing.UI.Controls">
    <GroupBox HorizontalAlignment="Stretch">
        <GroupBox.Header>Weather</GroupBox.Header>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Column="0" Grid.Row="0" Text="Air Temperature" />
            <ContentControl Grid.Column="1" Grid.Row="0" Content="{Binding AirTemperature.Current}" ContentStringFormat="{}{0:0.0}°C" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="0" Grid.Row="1" Text="Track Temperature" />
            <ContentControl Grid.Column="1" Grid.Row="1" Content="{Binding TrackTemperature.Current}" ContentStringFormat="{}{0:0.0}°C" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="0" Grid.Row="2" Text="Wind Speed" />
            <ContentControl Grid.Column="1" Grid.Row="2" Content="{Binding WindSpeed.Current}" ContentStringFormat="{}{0:0.00}m/s" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="0" Grid.Row="4" Text="Pressure" />
            <ContentControl Grid.Column="1" Grid.Row="4" Content="{Binding Pressure.Current}" ContentStringFormat="{}{0:0.0}mb" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="0" Grid.Row="5" Text="Humidity" />
            <ContentControl Grid.Column="1" Grid.Row="5" Content="{Binding Humidity.Current}" ContentStringFormat="{}{0:0}%" HorizontalAlignment="Right"/>
            <TextBlock Grid.Column="0" Grid.Row="6" Text="Raining" />
            <ContentControl Grid.Column="1" Grid.Row="6" Content="{Binding IsWet}" HorizontalAlignment="Right"/>
        </Grid>
    </GroupBox>
</UserControl>
